================================================================================
docs/nonprofit-corp-tax.html — 작업 이력
비영리 사단법인(학술단체) 세무 실무 노트 / 상담용 자료
================================================================================

[작업일자] 2026-09-08

[참고한 것 / 어디서부터 이었는지]
  - docs/annual-tax-flow.html (개인사업자 연간 세무 흐름도) — 이 저장소의 기존 산출물.
    색 토큰, 폰트, 글자 크기 세그먼트 컨트롤, 인쇄 CSS, zoom 스크립트를 그대로 승계했다.
    새로 만들지 않고 같은 조판 규칙 위에 얹은 것.
  - 기존 파일은 개인사업자 기준이라 비영리법인 내용이 전혀 없었음 → 별도 파일로 신설.

[작업요약]
  1. 신규 파일 docs/nonprofit-corp-tax.html 작성.
  2. 구성
     - 판정 흐름도 (SVG, viewBox 0 0 1760 780)
       · 트랙 1 = 법인세(법인세법) : 수익사업 유무 3갈래
         (없음 / 이자·배당소득만 → 분리과세 §62 / 있음 → 사업자등록·구분경리·3.31·준비금)
       · 트랙 2 = 공익법인(상증세법) : 전용계좌 → 4.30 네 가지 → 6.30 기부금영수증
       · 두 트랙이 서로 독립임을 시작 노드에서 인주색 화살표 2개로 표현
     - 수입 항목별 판정표 (법인세 / 부가세 2열 판정) ← 학술단체 특화 핵심 섹션
     - 연간 세무일정 표 (12월 결산 기준)
     - 사고가 나는 지점 카드 8개
     - 실무서·참고자료 5건
     - 신규 수임 시 받을 자료 체크리스트 14항목
  3. 학술단체(학회·포럼) 기준으로 전면 조정.
     핵심 메시지 = "부가세 면세 ≠ 법인세 비수익사업".
     참가비·게재료·광고료는 면세이면서 동시에 수익사업.
     부스 임대료·대관료는 실비 요건을 벗어나 부가세까지 과세.
     보조금(교부결정) vs 수탁연구용역(용역계약) 구분을 계약서철로 판정하도록 명시.

[사용코드 / 검증]
  # 태그 균형 검사
  python3 -c "from html.parser import HTMLParser ... "   # unclosed 0, errors 0

  # 렌더링 확인 (컨테이너 내장 Chromium)
  BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
  "$BIN" --headless --disable-gpu --no-sandbox --hide-scrollbars \
    --window-size=1800,2400 --screenshot=out.png --virtual-time-budget=6000 \
    "file:///home/user/claude/docs/nonprofit-corp-tax.html"

  # 하단 섹션만 보려면 임시 스타일을 덧붙여 상단을 숨기고 캡처
  { cat docs/nonprofit-corp-tax.html; \
    printf '<style>figure,.masthead,.page > .block:nth-of-type(1),.page > .block:nth-of-type(2){display:none}</style>'; } \
    > /tmp/np-bottom.html

  확인 결과 : SVG 노드 텍스트 겹침 없음, 표·카드·체크리스트 정상, 웹폰트 차단 시
  시스템 글꼴로 정상 대체됨(한글 깨짐 없음).

[보안 / 저장소 관련 주의]  ★중요
  - 이 저장소(wn0tkd0gus-hue/claude)는 2026-09-08 확인 시점에 **Public** 이다.
    (GitHub API visibility: public)
  - 따라서 이 파일에는 거래처명·법인명·담당자·금액 등 식별정보를 일절 넣지 않았다.
    문서는 "학술단체(학회·포럼) 일반" 기준으로만 작성했다.
  - 개별 법인의 판정 결과·수임 내역은 이 저장소에 커밋하지 말 것.
    저장소를 Private으로 전환한 뒤에야 개별 건 메모를 올릴 수 있다.

--------------------------------------------------------------------------------
[작업일자] 2026-09-08 (2차) — PDF · 워드 산출

[작업요약]
  1. 인쇄 CSS 수정
     - 기존: A4 가로 전체 + footer(면책 문구)가 인쇄에서 숨겨짐 → 10페이지, 표가 계속 잘림
     - 변경: 본문은 A4 세로, 판정 흐름도 한 장만 가로(CSS named page 사용)
         @page { size: A4 portrait; margin: 11mm 10mm; }
         @page wide { size: A4 landscape; margin: 8mm; }
         .masthead, figure { page: wide; }
       → 6페이지(p1 가로 = 표제+도표, p2~6 세로)
     - footer를 인쇄에 다시 노출(면책 문구는 인쇄물에 있어야 함)
     - 인쇄용 표 글자 13px, 카드 2단, 체크리스트 3단으로 축소
     - thead { display: table-header-group } — 표가 페이지 넘어가도 머리행 반복
  2. PDF 생성 (컨테이너 내장 Chromium)
  3. 워드(.docx) 생성 — docx-js. 도표는 PNG로 넣음(SVG는 워드가 못 읽음).

[사용코드]
  # (1) 도표만 PNG로 추출 — .page의 max-width 때문에 잘리므로 반드시 풀어줄 것
  #     별도 래퍼 HTML을 만들어 masthead/block/figfoot 숨기고 .page{max-width:none;width:1800px}
  BIN=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
  "$BIN" --headless --disable-gpu --no-sandbox --hide-scrollbars \
    --force-device-scale-factor=2 --window-size=1800,812 \
    --screenshot=diagram.png --virtual-time-budget=5000 "file://.../np-diagram.html"
  # → 3600 x 1624 (2배 해상도)

  # (2) PDF
  "$BIN" --headless --disable-gpu --no-sandbox --no-pdf-header-footer \
    --print-to-pdf="비영리 사단법인 세무 실무 노트.pdf" --virtual-time-budget=8000 \
    "file:///home/user/claude/docs/nonprofit-corp-tax.html"

  # (3) 워드
  npm install docx && node build-docx.js
  #   A4 가로 섹션(표제+도표) + A4 세로 섹션(본문) 2섹션 구조
  #   표는 columnWidths 합 = 표 width = 9638 DXA 로 맞출 것 (안 맞으면 워드에서 깨짐)
  #   음영은 ShadingType.CLEAR (SOLID 쓰면 검게 나옴)
  #   글꼴 '맑은 고딕'

  # (4) 인쇄 레이아웃 미리보기 — @media print 를 @media all 로 바꿔 폭 고정 후 캡처
  #     세로 본문 폭 = 210mm - 20mm ≈ 718px

[검증]
  - PDF : 6페이지, MediaBox로 p1 가로 / p2~6 세로 확인 (pymupdf로 판독)
  - DOCX: 이 컨테이너의 LibreOffice에는 Writer 모듈이 없어(soffice는 코어+pdfimport만)
          docx → pdf 변환이 불가능 → 렌더링 육안 확인은 못 함.
          대신 zip/XML 구조를 직접 파싱해 검증:
            섹션 2개(16838x11906 가로 / 11906x16838 세로), 표 2개(13x4, 17x3,
            열너비합 9638), 이미지 1개, 페이지나눔 3개, 문단 75개, 본문 4,699자,
            핵심 문구 10건 전수 존재 확인.
          → 워드에서 처음 열 때 도표 위치와 표 열폭은 한 번 눈으로 볼 것.

[산출물 위치]
  PDF·DOCX는 저장소에 커밋하지 않음. HTML이 원본이고 위 명령으로 언제든 재생성 가능하며,
  공개 저장소에 1.3MB 바이너리를 넣을 이유가 없다. 필요하면 Dropbox 쪽에 보관.

[다음단계]
  1. 저장소 Public → Private 전환 여부 결정. (전환 전까지 개별 법인 정보 커밋 금지)
  2. 수임 법인의 주무관청 확인 → 해당 부처 「비영리법인의 설립 및 감독에 관한 규칙」에서
     사업실적·결산서 제출 기한을 확인해 연간 세무일정 표의 "2~3월" 행을 확정치로 수정.
  3. 상증법상 공익법인 해당 여부, 기부금단체 지정 여부·지정기간 확인 후
     트랙 2 적용 여부를 문서에 반영(해당 없으면 트랙 2 섹션을 접는 형태로 조정).
  4. 학술대회 부스·광고 수입 규모 확인 → 부가세 과세분이 있으면
     면세사업장 현황신고(2.10)가 아니라 부가세 신고 체계로 일정표 조정.
  5. 과거 3년 신고서 검토 후 참가비·게재료 등 수익사업 누락분이 있으면
     수정신고·기한후신고 범위 산정. (별도 조서로 작성, 이 저장소에는 올리지 않음)

================================================================================
